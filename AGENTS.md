# AGENTS.md

本文件用于指导 AI 编程助手（Agent）在本仓库中的行为。

## 核心约定：讨论与结论必须先落盘

**所有与大模型 / AI Agent / RAG 等相关的讨论、分析、结论、方案设计、技术说明，都必须先以文档形式写入 `docs/` 目录，再在对话中给出总结。** 禁止只把结论留在聊天记录里而不落盘。

具体要求：

1. **落盘优先**：在给出最终结论前，先将完整内容写入 `docs/` 下的对应文档（新建或更新）。对话中的回复应是文档的摘要与链接，而非结论的唯一载体。
2. **分目录归类（按话题）**：不要将所有内容平铺在 `docs/` 根目录。`docs/` 下为每个一级话题建立独立子目录（命名用英文小写、短横线分隔，如 `rag`、`agents`、`llm`、`evaluation`），同一话题的所有文档归入对应子目录。
   - 每个话题目录以 `README.md` 作为该话题的索引/导航（汇总细分文档链接）。
   - `docs/` 根目录保留一个顶层 `README.md`，列出全部话题子目录及简介。
   - 新增话题：先建子目录与 `README.md`，再在顶层 `README.md` 登记。
3. **文档规范**：每篇文档应包含标题、目录、分点详述、示例代码/伪代码（如有）、以及相互引用的链接，保证可独立阅读。
4. **不遗漏中间过程**：重要的分析思路、对比、选型理由等中间讨论也应记录在文档中，而非仅记录最终结论。
5. **更新而非删除**：如需修订已有结论，采用增量更新并在文档中注明，保持 `docs/` 作为知识沉淀的单一可信源（single source of truth）。

### 目录结构示例

```
docs/
├── README.md            # 顶层索引：列出所有话题目录
├── rag/                 # 话题一：RAG
│   ├── README.md        # RAG 话题索引（导航细分文档）
│   ├── 01-基础RAG.md
│   ├── 02-进阶RAG.md
│   └── ...
├── agents/              # 话题二：AI Agent（按需创建）
│   └── README.md
└── llm/                 # 话题三：大模型基础（按需创建）
    └── README.md
```

> 目的：让 `docs/` 成为本仓库所有 AI 相关讨论与结论的持久化知识库，便于回溯、协作与持续演进。
